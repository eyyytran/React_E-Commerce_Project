import { Outlet } from 'react-router-dom'

const Collections = () => {
    return (
        <div className='collections-container'>
            <Outlet />
        </div>
    )
}

export default Collections
